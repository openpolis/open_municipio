## -*- mode: python -*-
## Use this module as the root URLconf for development

from open_municipio.urls import *

urlpatterns += patterns('',
    (r'^media/(?P<path>.*)$', 'django.views.static.serve', {'document_root': settings_local.MEDIA_ROOT}),
    (r'^static/(?P<path>.*)$', 'django.views.static.serve', {'document_root': settings_local.STATIC_ROOT}),
)